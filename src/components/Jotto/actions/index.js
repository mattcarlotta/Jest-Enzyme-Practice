import * as types from '../types';

/**
 * @function correctGuess
 * @returns {object} - Action object with type `CORRECT_GUESS`
 */
export const correctGuess = () => ({ type: types.CORRECT_GUESS });
